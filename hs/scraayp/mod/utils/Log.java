package hs.scraayp.mod.utils;

import hs.scraayp.mod.Mod;

public class Log {
    private Mod plugin = Mod.getPlugin(Mod.class);

    if(config.getString(player.getName() + ".item").equalsIgnoreCase("Iron")){

    }
}
